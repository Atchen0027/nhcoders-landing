---
import { DEVELOPER_INFO } from '../data/apps';
---

<section id="contacto" class="contact-section">
  <div class="container">
    <div class="contact-layout">
      <!-- Left: Developer Information & Trust Badges -->
      <div class="developer-info-card cyber-card reveal-on-scroll delay-1">
        <div class="card-badge-wrap">
          <span class="badge badge-mint">Contacto Directo & Presupuestos</span>
        </div>
        <h2 class="contact-heading">Hablemos de tu Proyecto</h2>
        <p class="contact-intro">
          ¿Buscas una web de alto rendimiento, automatizar citas o una app para Android? En <strong>AtchenDev / {DEVELOPER_INFO.brandName}</strong> te responderemos en menos de 24 horas con una propuesta clara.
        </p>

        <div class="info-items-list">
          <div class="info-item">
            <div class="info-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M20 21v-2a4 4 0 0 0-4-4H8a4 4 0 0 0-4 4v2"></path>
                <circle cx="12" cy="7" r="4"></circle>
              </svg>
            </div>
            <div class="info-text">
              <span class="info-label">Desarrollador</span>
              <strong class="info-val">Atchen Valladares (AtchenDev)</strong>
            </div>
          </div>

          <div class="info-item">
            <div class="info-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
                <circle cx="12" cy="10" r="3"></circle>
              </svg>
            </div>
            <div class="info-text">
              <span class="info-label">Ubicación</span>
              <strong class="info-val">{DEVELOPER_INFO.location}</strong>
            </div>
          </div>

          <div class="info-item">
            <div class="info-icon">
              <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
                <polyline points="22,6 12,13 2,6"></polyline>
              </svg>
            </div>
            <div class="info-text">
              <span class="info-label">Email Oficial</span>
              <a href={`mailto:${DEVELOPER_INFO.email}`} class="info-val info-link">{DEVELOPER_INFO.email}</a>
            </div>
          </div>
        </div>

        <!-- Trust Badges -->
        <div class="trust-grid">
          <div class="trust-pill">
            <span class="trust-icon">⚡</span>
            <span>Respuesta en menos de 24h</span>
          </div>
          <div class="trust-pill">
            <span class="trust-icon">🔒</span>
            <span>Presupuesto sin compromiso</span>
          </div>
          <div class="trust-pill">
            <span class="trust-icon">👾</span>
            <span>Código 100% limpio y optimizado</span>
          </div>
        </div>
      </div>

      <!-- Right: Direct Message Form -->
      <div class="contact-form-card cyber-card reveal-on-scroll delay-2">
        <h3 class="form-title">Pide tu Presupuesto o Consulta</h3>
        <p class="form-subtitle">Envíame tu idea y la analizaremos sin compromiso.</p>

        <form id="developer-contact-form" class="contact-form" action={`https://formsubmit.co/${DEVELOPER_INFO.email}`} method="POST">
          <input type="hidden" name="_subject" value="Nuevo mensaje / solicitud desde AtchenDev Portal" />
          <input type="hidden" name="_captcha" value="false" />
          <input type="hidden" name="_template" value="table" />

          <div class="form-group">
            <label for="name" class="form-label">Nombre o Empresa</label>
            <input type="text" id="name" name="name" required class="form-input" placeholder="Tu nombre o empresa" />
          </div>

          <div class="form-group">
            <label for="email" class="form-label">Email de Contacto</label>
            <input type="email" id="email" name="email" required class="form-input" placeholder="tu-email@ejemplo.com" />
          </div>

          <div class="form-group">
            <label for="appSelect" class="form-label">¿En qué puedo ayudarte?</label>
            <select id="appSelect" name="app" class="form-input form-select">
              <option value="Desarrollo Web / Landing Page">Desarrollo Web / Landing Page</option>
              <option value="Sistema de Reservas / Gestor ERP">Sistema de Reservas / Gestor ERP</option>
              <option value="Desarrollo App Android">Desarrollo App Android</option>
              <option value="Tienda Online / E-Commerce">Tienda Online / E-Commerce</option>
              <option value="Optimización Web / PageSpeed">Optimización & Mantenimiento Web</option>
              <option value="Soporte App Android NHCoders">Soporte App Android (NHCoders)</option>
              <option value="Consulta General">Consulta General / Otros</option>
            </select>
          </div>

          <div class="form-group">
            <label for="message" class="form-label">Detalles del Proyecto o Mensaje</label>
            <textarea id="message" name="message" rows="3" required class="form-input" placeholder="Cuéntame brevemente qué necesitas o qué problema quieres resolver..."></textarea>
          </div>

          <div id="form-feedback" class="form-feedback" style="display: none;"></div>

          <button type="submit" id="form-submit-btn" class="btn btn-mint form-submit-btn">
            <svg width="16" height="16" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5">
              <line x1="22" y1="2" x2="11" y2="13"></line>
              <polygon points="22 2 15 22 11 13 2 9 22 2"></polygon>
            </svg>
            <span>Enviar Solicitud</span>
          </button>
        </form>
      </div>
    </div>
  </div>
</section>

<script>
  // Asynchronous Form Submission
  const form = document.getElementById('developer-contact-form') as HTMLFormElement | null;
  const feedbackEl = document.getElementById('form-feedback');
  const submitBtn = document.getElementById('form-submit-btn') as HTMLButtonElement | null;

  if (form && feedbackEl && submitBtn) {
    form.addEventListener('submit', async (e) => {
      e.preventDefault();
      
      const formData = new FormData(form);
      const originalBtnHtml = submitBtn.innerHTML;

      submitBtn.disabled = true;
      submitBtn.innerHTML = `<span>Enviando... ⏳</span>`;
      feedbackEl.style.display = 'none';

      try {
        const response = await fetch(form.action.replace('https://formsubmit.co/', 'https://formsubmit.co/ajax/'), {
          method: 'POST',
          headers: {
            'Content-Type': 'application/json',
            'Accept': 'application/json'
          },
          body: JSON.stringify(Object.fromEntries(formData.entries()))
        });

        if (response.ok) {
          feedbackEl.className = 'form-feedback success';
          feedbackEl.innerHTML = `✅ <strong>¡Mensaje enviado con éxito!</strong> Me pondré en contacto contigo en menos de 24 horas.`;
          feedbackEl.style.display = 'block';
          form.reset();
        } else {
          throw new Error('Respuesta no exitosa');
        }
      } catch (err) {
        feedbackEl.className = 'form-feedback success';
        feedbackEl.innerHTML = `✅ <strong>¡Enviado!</strong> Mensaje recibido correctamente.`;
        feedbackEl.style.display = 'block';
        form.submit();
      } finally {
        submitBtn.disabled = false;
        submitBtn.innerHTML = originalBtnHtml;
      }
    });
  }
</script>

<style>
  .contact-section {
    padding: 5rem 0 6rem;
    contain: content;
  }

  /* 2-Column Grid Layout */
  .contact-layout {
    display: grid;
    grid-template-columns: repeat(2, 1fr);
    gap: 2rem;
    width: 100%;
  }

  .developer-info-card, .contact-form-card {
    padding: 2.5rem 2.25rem;
    display: flex;
    flex-direction: column;
    background: var(--bg-card);
    border: 1px solid var(--border-card);
    border-radius: var(--radius-lg);
    min-width: 0;
  }

  .card-badge-wrap {
    margin-bottom: 0.75rem;
  }

  .contact-heading {
    font-size: 1.65rem;
    margin-bottom: 0.5rem;
    line-height: 1.2;
  }

  .contact-intro {
    font-size: 0.92rem;
    margin-bottom: 1.75rem;
    line-height: 1.6;
  }

  .contact-intro strong {
    color: var(--nhc-mint);
  }

  .info-items-list {
    display: flex;
    flex-direction: column;
    gap: 1.15rem;
    margin-bottom: 2rem;
  }

  .info-item {
    display: flex;
    align-items: center;
    gap: 0.85rem;
    min-width: 0;
  }

  .info-icon {
    width: 40px;
    height: 40px;
    border-radius: var(--radius-sm);
    background: rgba(0, 245, 184, 0.12);
    border: 1px solid rgba(0, 245, 184, 0.3);
    color: var(--nhc-mint);
    display: flex;
    align-items: center;
    justify-content: center;
    flex-shrink: 0;
  }

  .info-text {
    min-width: 0;
    overflow: hidden;
  }

  .info-label {
    display: block;
    font-size: 0.72rem;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    color: var(--text-muted);
    font-weight: 700;
  }

  .info-val {
    font-size: 0.92rem;
    color: #ffffff;
    word-break: break-word;
  }

  .info-link {
    color: var(--nhc-mint);
    transition: color var(--transition-fast);
  }

  .info-link:hover {
    color: #ffffff;
    text-decoration: underline;
  }

  .trust-grid {
    margin-top: auto;
    display: flex;
    flex-direction: column;
    gap: 0.6rem;
    padding-top: 1.5rem;
    border-top: 1px solid var(--border-subtle);
  }

  .trust-pill {
    display: flex;
    align-items: center;
    gap: 0.6rem;
    font-size: 0.85rem;
    color: var(--text-secondary);
  }

  /* Form */
  .form-title {
    font-size: 1.45rem;
    margin-bottom: 0.35rem;
  }

  .form-subtitle {
    font-size: 0.88rem;
    margin-bottom: 1.5rem;
    color: var(--text-secondary);
  }

  .contact-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    gap: 0.35rem;
  }

  .form-label {
    font-size: 0.82rem;
    font-weight: 700;
    color: var(--text-secondary);
    font-family: var(--font-heading);
  }

  .form-input {
    width: 100%;
    padding: 0.7rem 0.9rem;
    background: rgba(5, 8, 18, 0.85);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: var(--radius-sm);
    color: var(--text-primary);
    font-family: var(--font-body);
    font-size: 0.9rem;
    transition: border-color var(--transition-fast), box-shadow var(--transition-fast);
    box-sizing: border-box;
  }

  .form-input:focus {
    outline: none;
    border-color: var(--nhc-mint);
    box-shadow: 0 0 0 3px rgba(0, 245, 184, 0.2);
  }

  .form-select {
    cursor: pointer;
  }

  .form-feedback {
    padding: 0.75rem 1rem;
    border-radius: var(--radius-sm);
    font-size: 0.86rem;
  }

  .form-feedback.success {
    background: rgba(0, 245, 184, 0.12);
    border: 1px solid var(--nhc-mint);
    color: #e2e8f0;
  }

  .form-submit-btn {
    margin-top: 0.35rem;
    width: 100%;
    padding: 0.85rem;
    font-size: 0.95rem;
  }

  /* Mobile: 2-Column Grid adaptation */
  @media (max-width: 768px) {
    .contact-section {
      padding: 2.5rem 0 4rem;
    }

    .contact-layout {
      grid-template-columns: repeat(2, 1fr);
      gap: 0.6rem;
    }

    .developer-info-card, .contact-form-card {
      padding: 1.15rem 0.85rem;
      border-radius: var(--radius-sm);
    }

    .contact-heading {
      font-size: 1.1rem;
      margin-bottom: 0.35rem;
    }

    .contact-intro {
      font-size: 0.78rem;
      margin-bottom: 0.9rem;
    }

    .info-items-list {
      gap: 0.65rem;
      margin-bottom: 1rem;
    }

    .info-icon {
      width: 30px;
      height: 30px;
    }

    .info-icon svg {
      width: 14px;
      height: 14px;
    }

    .info-label {
      font-size: 0.62rem;
    }

    .info-val {
      font-size: 0.78rem;
    }

    .trust-grid {
      padding-top: 0.85rem;
      gap: 0.4rem;
    }

    .trust-pill {
      font-size: 0.7rem;
      gap: 0.35rem;
    }

    .form-title {
      font-size: 1.1rem;
      margin-bottom: 0.2rem;
    }

    .form-subtitle {
      font-size: 0.74rem;
      margin-bottom: 0.85rem;
    }

    .contact-form {
      gap: 0.65rem;
    }

    .form-label {
      font-size: 0.72rem;
    }

    .form-input {
      padding: 0.45rem 0.55rem;
      font-size: 0.78rem;
    }

    .form-submit-btn {
      padding: 0.6rem;
      font-size: 0.8rem;
    }
  }

  @media (max-width: 380px) {
    .contact-layout {
      gap: 0.4rem;
    }
    .developer-info-card, .contact-form-card {
      padding: 0.85rem 0.55rem;
    }
  }
</style>
